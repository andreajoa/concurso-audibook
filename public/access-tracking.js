(()=>{const sid=new URLSearchParams(location.search).get('session_id')||'';if(!sid)return;function general(name,extra={}){window.concursoCRM?.track?.(name,{stripe_session_id:sid,...extra},true)}function metric(name){fetch('/api/content-metric',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({session_id:sid,event:name}),keepalive:true}).catch(()=>{})}function fire(name,extra){general(name,extra);if(['access_open','pdf_download','audio_play','audio_download'].includes(name))metric(name)}function ready(){fire('access_open');general('pdf_open');const pdfDownload=document.querySelector('.portal-title a.button-link');if(pdfDownload)pdfDownload.addEventListener('click',()=>fire('pdf_download'));document.querySelectorAll('.track-download').forEach((a,i)=>a.addEventListener('click',()=>fire('audio_download',{metadata:{track_index:i}})));const player=document.getElementById('portal-player');if(player)player.addEventListener('play',()=>fire('audio_play',{metadata:{current_src:player.currentSrc||''}}));const openPdf=document.querySelector('.tabs a');if(openPdf)openPdf.addEventListener('click',()=>general('pdf_open'))}if(window.concursoCRM)ready();else window.addEventListener('load',()=>setTimeout(ready,80),{once:true})})();