(()=>{
const form=document.getElementById('checkout-form');if(!form)return;
const lead=document.getElementById('lead-stage'),stage=document.getElementById('embedded-stage'),target=document.getElementById('embedded-checkout'),loading=document.getElementById('embedded-loading'),err=document.getElementById('checkout-error'),edit=document.getElementById('edit-buyer');
const slug=new URLSearchParams(location.search).get('produto')||'autores-ibam-2026';
const steps=[...document.querySelectorAll('[data-step]')];let checkout=null,cfgPromise=null;
const identity=()=>window.concursoCRM?.identity?.()||{};
const setStep=n=>steps.forEach(x=>x.classList.toggle('is-active',Number(x.dataset.step)<=n));
const getConfig=()=>cfgPromise||(cfgPromise=fetch('/api/checkout',{cache:'no-store'}).then(async r=>{const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Pagamento indisponível');return d}));
function loadLibrary(){if(window.Stripe)return Promise.resolve();return new Promise((resolve,reject)=>{const old=document.querySelector('script[data-payment-library]');if(old){old.addEventListener('load',resolve,{once:true});old.addEventListener('error',reject,{once:true});return}const s=document.createElement('script');s.src='https://js.stripe.com/v3/';s.async=true;s.dataset.paymentLibrary='1';s.onload=resolve;s.onerror=()=>reject(new Error('Biblioteca de pagamento indisponível'));document.head.appendChild(s)})}
async function reset(){if(checkout){try{checkout.destroy()}catch{}checkout=null}target.innerHTML='';loading.hidden=false;stage.hidden=true;lead.hidden=false;setStep(1);err.textContent='';const b=form.querySelector('button[type="submit"]');b.disabled=false;b.textContent='Continuar para o pagamento seguro'}
form.addEventListener('submit',async e=>{e.preventDefault();const b=form.querySelector('button[type="submit"]'),name=form.elements.name.value.trim(),email=form.elements.email.value.trim().toLowerCase(),whatsapp=form.elements.whatsapp.value.replace(/\D/g,''),marketing=form.elements.marketing.checked;if(name.length<3||!/^\S+@\S+\.\S+$/.test(email)||whatsapp.length<10){err.textContent='Confira nome, e-mail e WhatsApp antes de continuar.';return}b.disabled=true;b.textContent='Preparando pagamento seguro…';err.textContent='';try{const cfg=await getConfig();if(!cfg.embedded||!String(cfg.publishableKey||'').startsWith('pk_'))throw new Error('O pagamento seguro está temporariamente indisponível.');window.concursoCRM?.track('lead_submitted',{product_slug:slug,value_cents:2499});window.concursoCRM?.track('checkout_started',{product_slug:slug,value_cents:2499});const r=await fetch('/api/checkout',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({slug,name,email,whatsapp,marketingConsent:marketing,analytics:identity(),embedded:true})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Não foi possível iniciar o pagamento.');if(d.mode!=='embedded'||!d.clientSecret)throw new Error('O pagamento dentro da página não pôde ser iniciado.');await loadLibrary();lead.hidden=true;stage.hidden=false;setStep(2);stage.scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth',block:'start'});checkout=await window.Stripe(cfg.publishableKey).initEmbeddedCheckout({clientSecret:d.clientSecret});loading.hidden=true;checkout.mount('#embedded-checkout')}catch(error){console.error(error);err.textContent=error.message&&error.message.length<180?error.message:'Não foi possível abrir o pagamento agora. Tente novamente.';b.disabled=false;b.textContent='Continuar para o pagamento seguro';lead.hidden=false;stage.hidden=true;setStep(1)}});
edit?.addEventListener('click',reset);getConfig().catch(()=>null);
})();
