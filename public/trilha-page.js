(()=>{const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const hero=document.querySelector('.catalog-hero');const cover=document.querySelector('.catalog-cover');const trail=document.querySelector('[data-trail-map]');const stops=[...document.querySelectorAll('[data-trail-stop]')];const format=document.querySelector('.format-stage');document.documentElement.classList.add('reveal-ready');const revealTargets=[...document.querySelectorAll('[data-sc-object],.evidence-grid article,.format-stage,.future-card')];const reveal=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('is-visible')}),{threshold:.16});revealTargets.forEach(el=>reveal.observe(el));if(reduce){stops.forEach(x=>x.classList.add('is-active'));return}let ticking=false;function update(){ticking=false;const vh=innerHeight||1;if(hero&&cover){const r=hero.getBoundingClientRect();const p=Math.max(0,Math.min(1,(vh-r.top)/(vh+r.height)));hero.style.setProperty('--cover-turn',p.toFixed(3));hero.style.setProperty('--cover-lift',p.toFixed(3))}if(trail){const r=trail.getBoundingClientRect();const p=Math.max(0,Math.min(1,(vh*.72-r.top)/(Math.max(1,r.height-vh*.34))));trail.style.setProperty('--trail-progress',p.toFixed(3));stops.forEach((stop,i)=>stop.classList.toggle('is-active',p>=(i/(stops.length-1||1))-.08))}if(format){const r=format.getBoundingClientRect();const p=Math.max(0,Math.min(1,(vh-r.top)/(vh+r.height)));format.style.setProperty('--format-shift',p.toFixed(3))}}function onScroll(){if(!ticking){ticking=true;requestAnimationFrame(update)}}addEventListener('scroll',onScroll,{passive:true});addEventListener('resize',onScroll,{passive:true});update();})();