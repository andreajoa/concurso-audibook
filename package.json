{
  "name": "concurso-audiobook",
  "version": "1.1.0",
  "private": true,
  "description": "Biblioteca de estudos com checkout, acesso individual, apostilas e audiobooks.",
  "scripts": {
    "build": "node scripts/cloudflare-normalize.mjs && node scripts/validate-portal-images.mjs && node scripts/build-seo.cjs && node scripts/inject-portal-shell-v2.mjs && npm test && npm run verify && npm run verify:seo && npm run verify:scrollcraft && bash scripts/build-assets.sh",
    "inject:portal": "node scripts/inject-portal-shell-v2.mjs",
    "validate:portal-images": "node scripts/validate-portal-images.mjs",
    "verify": "node scripts/verify.mjs",
    "verify:seo": "node scripts/verify-seo.mjs",
    "verify:scrollcraft": "node scripts/verify-scrollcraft.mjs",
    "verify:remote": "node scripts/verify-remote.mjs",
    "check:concursos": "node scripts/check-concursos.mjs",
    "check:concursos:offline": "node scripts/check-concursos.mjs --offline",
    "test": "node --test tests/*.test.cjs"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.883.0",
    "@aws-sdk/s3-request-presigner": "^3.883.0",
    "nodemailer": "^7.0.6"
  }
}
