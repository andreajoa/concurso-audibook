import fs from 'node:fs';import path from 'node:path';const root=process.cwd();let failed=false;const ok=m=>console.log('SCROLLCRAFT OK  '+m),fail=m=>{failed=true;console.error('SCROLLCRAFT FAIL '+m)};const read=f=>fs.readFileSync(path.join(root,f),'utf8');const html=read('public/index.html'),css=read('public/trilha.css')+'\n'+read('public/experience.css'),js=read('public/trilha-page.js')+'\n'+read('public/experience.js'),vercel=read('vercel.json');for(const f of ['scrollcraft/BRIEF.md','scrollcraft/FINGERPRINTS.md','public/assets/apostila-santos-ibam-3d.webp','public/assets/trilha-aprova-logo.webp','public/experience.css','public/experience.js'])fs.existsSync(path.join(root,f))?ok(f):fail('missing '+f);const coverPath=path.join(root,'public/assets/apostila-santos-ibam-3d.webp');if(fs.existsSync(coverPath)&&fs.statSync(coverPath).size<30000)fail('3D cover asset is suspiciously small/blank');else ok('3D cover binary has expected visual asset size');const heroPos=html.indexOf('class="catalog-hero');const genericHeroPos=html.indexOf('class="ta-hero');if(heroPos<0)fail('catalog hero missing');else ok('gallery/catalog object is the hero');if(genericHeroPos>=0&&genericHeroPos<heroPos)fail('generic hero appears before product object');if(!html.includes('src="/assets/apostila-santos-ibam-3d.webp?v=bf874874'))fail('restored 3D cover URL is not correctly versioned');else ok('restored 3D cover is cache-versioned');if(!html.includes('fetchpriority="high"')||!html.includes('loading="eager"'))fail('hero cover priority missing');if(!html.includes('data-trail-map')||!html.includes('data-trail-stop'))fail('signature Trilha de Estudo markup missing');else ok('signature move markup present');if(!js.includes('--trail-progress')||!js.includes('prefers-reduced-motion'))fail('signature move behavior or reduced-motion handling missing');else ok('signature move + reduced-motion behavior present');if(!html.includes('data-stack-card')||!css.includes('.stack-card'))fail('stacking cards device missing');else ok('stacking cards device present');if(!html.includes('data-scroll-fill')||!js.includes('fillWords'))fail('scroll-filled text device missing');else ok('scroll-filled text device present');if(!html.includes('data-expand-panel')||!js.includes('openPanel'))fail('expanding panels device missing');else ok('expanding panels device present');if(!css.includes('@media(max-width:600px)')&&!css.includes('@media(max-width:640px)'))fail('mobile art direction missing');else ok('separate mobile composition present');if(!css.includes('@media(prefers-reduced-motion:reduce)'))fail('reduced-motion CSS missing');else ok('reduced-motion CSS present');const ctas=[...html.matchAll(/>Comprar esta apostila</g)].length;if(ctas<3)fail('primary CTA is not repeated consistently');else ok('single purchase label repeated consistently');if(!/<del>R\$ 49,99<\/del>/.test(html))fail('reference price is not crossed out with semantic del');if(/immutable/.test(vercel))fail('mutable storefront assets still use immutable cache');else ok('asset cache can revalidate');for(const token of ['catalog-hero','specimen-strip','stack-cards','trail-map','expand-panels','catalog-close'])if(!html.includes(token))fail('page grammar missing '+token);if(!read('scrollcraft/FINGERPRINTS.md').includes('PASS'))fail('fingerprint gate not recorded');else ok('fingerprint gate recorded');if(failed)process.exit(1);console.log('\nScroll Craft structural verification passed.');