const crypto=require('crypto');
const {unsubscribeToken}=require('../lib/email');
const {rpc}=require('../lib/crm-rpc');
module.exports=async(req,res)=>{try{const email=String(req.query?.email||'').trim().toLowerCase(),token=String(req.query?.token||'');const expected=unsubscribeToken(email);if(!email||!token||!expected){res.statusCode=400;return res.end('Link de cancelamento inválido.')}const a=Buffer.from(token),b=Buffer.from(expected);if(a.length!==b.length||!crypto.timingSafeEqual(a,b)){res.statusCode=403;return res.end('Link de cancelamento inválido.')}await rpc('crm_set_unsubscribe',{p_email:email,p_reason:'link_email'});res.setHeader('Content-Type','text/html; charset=utf-8');res.setHeader('Cache-Control','private, no-store');return res.status(200).send('<!doctype html><html lang="pt-BR"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><body style="margin:0;background:#fff8f3;font-family:Arial;color:#13243a"><main style="max-width:560px;margin:12vh auto;padding:28px;background:#fff;border:1px solid #e3d9cf;border-radius:20px"><h1 style="font-family:Georgia,serif;font-weight:400">Preferência atualizada.</h1><p>Você não receberá mais e-mails promocionais desta sequência. Mensagens essenciais relacionadas a uma compra ou recuperação de acesso ainda podem ser enviadas quando necessário.</p><a href="/comprar.html" style="color:#a64b2a">Voltar ao site</a></main></body></html>');}catch(e){console.error('unsubscribe',e);return res.status(500).end('Não foi possível atualizar sua preferência agora.');}};
